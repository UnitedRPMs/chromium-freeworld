mock_travis:
mock_config: fedora-24-x86_64
packages_extra_repo: https://sourceforge.net/projects/unitedrpms/files/25/x86_64/

sudo: required

services:
  - docker

script:
  - wget -q https://github.com/nrechn/mock-travis/releases/download/latest/mock-travis 
        && chmod +x mock-travis 
        && ./mock-travis

