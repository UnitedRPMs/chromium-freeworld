--- a/build/config/compiler/BUILD.gn	2019-03-14 04:38:43.743689762 -0600
+++ b/build/config/compiler/NEW		2019-03-14 11:32:29.678340517 -0600
@@ -1659,7 +1659,7 @@
   # have a "thin archive" mode (it does accept -T, but it means truncating
   # archive names to 16 characters, which is not what we want).
   if ((is_posix && !is_nacl && !is_mac && !is_ios) || is_fuchsia) {
-    arflags = [ "-T" ]
+    arflags = [ "T" ]
   } else if (is_win && use_lld) {
     arflags = [ "/llvmlibthin" ]
   }

--- a/build/toolchain/gcc_toolchain.gni	2019-03-11 16:00:52.000000000 -0600
+++ b/build/toolchain/NEW		2019-03-14 11:35:48.852526920 -0600
@@ -351,7 +351,7 @@
       } else {
         rspfile = "{{output}}.rsp"
         rspfile_content = "{{inputs}}"
-        command = "\"$ar\" {{arflags}} -r -c -s -D {{output}} @\"$rspfile\""
+        command = "\"$ar\" rcsD{{arflags}} {{output}} @\"$rspfile\""
       }
 
       # Remove the output file first so that ar doesn't try to modify the
